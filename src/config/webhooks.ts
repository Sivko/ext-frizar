// Конфигурация webhook'ов
// Импортируем эндпоинты из отдельного файла

import { getWebhookUrl } from './endpoints'

// Экспортируем функцию для использования в background script
export { getWebhookUrl }
